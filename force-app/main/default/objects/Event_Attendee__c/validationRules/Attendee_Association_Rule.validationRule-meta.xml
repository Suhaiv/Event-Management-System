<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Attendee_Association_Rule</fullName>
    <active>false</active>
    <errorConditionFormula>OR(
 Event__r.Live__c = FALSE,
 DATEVALUE(Event__r.End__c) &lt;= TODAY(),
 Event__r.Remaining_Seats__c &lt;= 0
)</errorConditionFormula>
    <errorMessage>You cannot register for this event because it is either not live, already ended, or has no available seats.</errorMessage>
</ValidationRule>
